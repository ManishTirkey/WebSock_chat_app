version: "3.1"

services:
  chatApp:
    build: Dockerfile
    ports:
      - "80:8000"
    depends_on:
      - db

  db:
    image: postgres
    restart: always
    env_file:
      - .env
    volumes:
      - . .
    ports:
      - 5432:5432
